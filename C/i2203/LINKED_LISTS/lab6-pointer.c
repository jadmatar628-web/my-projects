#include<stdio.h>
#include<stdlib.h>

int main()
{
    printf("Enter the size of your array: ");
    int size;
    scanf("%d",&size);
    int *arr;
    arr=malloc(size*sizeof(int));
    for(int i=0;i<size;i++)
    {
        scanf("%d",arr+)
    }
}